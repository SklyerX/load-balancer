# test-servers

The chaos fleet for the load balancer in the parent directory. Four fake
backends, a control plane on `:9999`, and a load generator.

Documented in the root [README](../README.md#the-test-fleet).

```bash
pnpm install
pnpm dev
```
